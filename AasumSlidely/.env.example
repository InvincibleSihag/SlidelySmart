# Backend API proxy target (used by Vite dev server proxy, not exposed to browser)
# In Docker: http://web:9753 | Outside Docker: http://localhost:9753
# API_PROXY_TARGET=http://localhost:9753

# Pusher (for real-time job status notifications)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=ap2
