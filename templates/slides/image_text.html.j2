{% from "_macros.html.j2" import render_element, slide_bg, el_attrs, element_style %}
<div class="slide slide-image-text" data-slide-id="{{ slide.id }}"{{ slide_bg(slide) }}>
  {% for element in slide.elements %}
    {% if element.type in ("heading", "title") %}
      <h2 class="slide-heading" {{ el_attrs(element) }} {{ element_style(element) }}>{{ element.content | rich_text }}</h2>
    {% endif %}
  {% endfor %}
  <div class="image-text-layout">
    <div class="image-side">
      {% for element in slide.elements %}
        {% if element.type == "image" %}
          {{ render_element(element) }}
        {% endif %}
      {% endfor %}
    </div>
    <div class="text-side">
      {% for element in slide.elements %}
        {% if element.type not in ("heading", "title", "image") %}
          {{ render_element(element) }}
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>
